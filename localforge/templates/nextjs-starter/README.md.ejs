# <%= appName %>

This project was scaffolded dynamically by LocalForge App Blueprint Generator.

## App Architecture
- **Type**: <%= appType %>
- **Pages**: <%= pages.length %> configured
- **API Routes**: <%= apiRoutes.length %> configured
- **Entities**: <%= entities.length %> configured in Prisma schema

## Next Steps
1. Run \`pnpm install\` or \`npm install\`
2. Run \`npx prisma db push\`
3. Run \`npm run dev\`
